class Module {
  constructor(moduleCode, title, type, completionDate, level) {
    this.moduleCode = moduleCode
    this.title = title
    this.type = type
    this.completionDate = completionDate
    this.level = level
  }
}

export default Module
